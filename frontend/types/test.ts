import { ResponseList } from ".";
import { BOARD_TYPE, BoardPageOptions, BoardType, RequestBoard } from "./board";

const test1: RequestBoard = {
  boardType: "12",
  content: ' "asd',
  title: "asd",
};

console.log(test);
